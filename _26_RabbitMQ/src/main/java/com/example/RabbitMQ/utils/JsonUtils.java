/*
package com.example.RabbitMQ.utils;

import com.example.RabbitMQ.common.Message;

import java.io.IOException;

public class JsonUtils {
    public static String jsonStringFromObject(Object object) throws JsonProcessingException {
        ObjectMapper mapper = new ObjectMapper();
        return mapper.writeValueAsString(object);
    }

    public static Message objectFromJsonString(String jsonStr) throws IOException {
        ObjectMapper mapper = new ObjectMapper();
        return mapper.readValue(jsonStr, Message.class);
    }
}
*/
