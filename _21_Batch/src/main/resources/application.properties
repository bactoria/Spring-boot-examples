#####           postgres 생성       ###########
# docker run -p 5432:5432 -e POSTGRES_PASSWORD=pass -e POSTGRES_USER=bactoria -e POSTGRES_DB=springBatch --name batchTest -d postgres
#
# docker exec -it batchTest bash 는 안해도 됨.
# datagrip 으로 확인하자.
################################

spring.datasource.url=jdbc:postgresql://localhost:5432/springBatch
spring.datasource.username=bactoria
spring.datasource.password=pass

spring.jpa.properties.hibernate.jdbc.lob.non_contextual_creation=true 


# postgresql 을 이용하려면 schema 테이블들이 있어야 함.
# schema-postgresql.sql 을  double shift로 검색해서
# 직접 넣어주자. data grip 이용


# 특정 job만 실행
spring.batch.job.names= ${job.name:none}
# ex) --job.name=paramsJob  ==> paramsJob 만 실행하겠다.
# ${job.name:none} ==> job.name 값이 없으면 값을 none으로 하겠다.
